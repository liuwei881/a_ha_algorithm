#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "sort.h"

int main()
{
	//bubbling();
	//bubbling_student();
	/*
	int i;
	printf("请输入数组的大小: ");
	scanf("%d", &n);
	for (i = 1; i <= n; i++)
	{
		scanf("%d", &a[i]);
	}
	quicksort(1, n);
	for (i = 1; i <= n; i++)
	{
		printf("%d ", a[i]);
	}
	*/
	//bucket_sort();
	sort_out();
	return 0;
}